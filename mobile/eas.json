{
  "cli": {
    "version": ">= 10.0.0",
    "appVersionSource": "local"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "prebuildCommand": "npx expo prebuild --clean && node patch-gradle-after-prebuild.js"
    },
    "preview": {
      "distribution": "internal",
      "prebuildCommand": "npx expo prebuild --clean && node patch-gradle-after-prebuild.js",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "prebuildCommand": "npx expo prebuild --clean && node patch-gradle-after-prebuild.js"
    }
  },
  "submit": {
    "production": {}
  }
}
